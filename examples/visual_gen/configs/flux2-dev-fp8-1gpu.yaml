# 1-GPU FLUX.2-dev with FP8 dynamic quantization.
quant_config:
  quant_algo: FP8
  dynamic: true
attention_config:
  backend: VANILLA
parallel_config:
  cfg_size: 1
  ulysses_size: 1
cuda_graph_config:
  enable: false
